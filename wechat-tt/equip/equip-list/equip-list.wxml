<!--equip/equip/equip.wxml-->
<import src='/template/post.wxml'/>
<view class='equip'>
  <!-- <view class='equip-title'>
    <navigator url='/equip/equip-filter/equip-filter' class='filter'>
      <image class='icon-filter' src='/images/equip-filter.png'></image>
      <view>{{filterCon}}</view>
    </navigator>
    <view catchtap='changeCheck' class='{{isChecked?"checked":"check"}}'>
      <image wx:if='{{isChecked}}' class='checked-img' src='/images/icon-success.png'></image>
      <view wx:else class='check-img'></view>
      <view>已认证</view> 
    </view>
  </view> -->
  <view>
    <block wx:for='{{equipList}}' wx:key='index'>
      <view catchtap='goEquipDetail' data-club-id='{{item.clubId}}' data-club-name='{{item.clubName}}' data-club-logo='{{item.clubLogo}}' data-id='{{item.equipId}}'>
        <template is='equip' data='{{...item,curUserId}}'/>
      </view>
    </block>
    <view wx:if='{{equipList.length==0}}' class='empty-wrap'>
      <image class='icon-empty-default' src='/images/icon-empty-default.png'></image>
      <view>暂无信息</view>
    </view>
    <view wx:elif='{{showAll==true}}' class='show-all'>已到最后</view>
    <view wx:elif='{{showAll=="加载中..."}}' class='show-all'>加载中...</view>
    
    <!-- <navigator url='/equip/equip-detail/equip-detail'>
      <template is='equip'/>
    </navigator> -->
  </view>
</view>
<button open-type='getUserInfo' bindgetuserinfo='toMyEquip' class='icon-join'>
  <image src='/images/icon-equip.png'></image>
</button>
<Share id='share' shareparams='{{curEquip}}' sharetype='equip' />
<canvas canvas-id="myCanvas" style="position:absolute;left:-200%;width:100%;height:420rpx;"></canvas>
